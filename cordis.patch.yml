- insert:
    - id: yuque-notes
      name: yuque-notes-plugin
      config:
        bookName: 我的笔记
        token: !!js process.env.YUQUE_TOKEN
        imageHosting:
          provider: github
          repo: !!js (process.env.YUQUE_IMAGE_REPO || 'cchenbin042/yuque-notes-images')
          token: !!js process.env.GITHUB_TOKEN
